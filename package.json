{
  "name": "create-react-app-blog",
  "version": "0.1.4",
  "description": "create-react-app plugin for a Jekyll style Markdown blog",
  "main": "./distribution/index.js",
  "scripts": {
    "clean": "rm -rf distribution",
    "build": "npm run clean && babel source --presets babel-preset-es2015,babel-preset-react --out-dir distribution",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/sanfrancesco/create-react-app-blog.git"
  },
  "author": "jonathan.otto@gmail.com",
  "license": "ISC",
  "dependencies": {
    "github-markdown-css": "^2.4.1",
    "markdown-with-front-matter-loader": "^0.1.0"
  },
  "devDependencies": {
    "babel-cli": "^6.16.0",
    "babel-preset-es2015": "^6.16.0",
    "babel-preset-react": "^6.16.0"
  }
}
